# orchestrate-fix profile — homebridge-sun-azimuth
<!-- Generated by orchestrate-fix-init on 2026-07-02. Re-run to refresh. -->

## Ticket source
- System: none (plain descriptions)
- Read a ticket: n/a — work is spec'd directly by the user; informal history lives in `CHANGELOG.md`
- ID format: n/a

## Isolation
- Strategy: worktrees (Claude Code's built-in `.claude/worktrees/<name>` mechanism)
- Setup script: n/a — use the `EnterWorktree` tool directly, no repo-side setup script
- Branch naming: no enforced convention. Precedent: either Claude Code's auto `worktree-<random-words>` name, or a short descriptive name (e.g. `suncalc-2-upgrade`). Prefer a short descriptive branch name for clarity.
- Default branch (never push here): `master`

## Build gate (must pass before PR)
- Blocking: none — no `scripts` key in `package.json`, no CI config, no TypeScript/ESLint/Prettier config in the repo. Plain CommonJS JavaScript (not TypeScript despite folder naming conventions elsewhere). Sanity check: `node -e "require('./index.js')"` or `node --check <file>` for syntax validity is the closest thing to a gate.
- Advisory: n/a. Note `nodemon.json` exists (watches `base/` + `index.js`, execs `homebridge -I -D`) but `nodemon` isn't a devDependency and no script wires it up — treat as a manual dev-loop hint, not a gate.

## Run / verify harness
- How to exercise a change: manual only. No automated test suite exists (no `test/`, no `*.test.js`, no jest/mocha config). To verify: install Homebridge locally, run `nodemon` (or `homebridge -I -D` directly) with this plugin configured, and observe accessory/platform behavior (sun azimuth/elevation values) in Homebridge logs.
- Auth/boot for verifier: n/a — no external services; requires a local Homebridge instance and a config block for this plugin (see `README.md` for config schema).

## Code conventions
- Rule sources: none formally documented (no `CLAUDE.md`, `.eslintrc*`, `.prettierrc*`, `tsconfig.json`, `CONTRIBUTING.md`). Infer style by reading neighboring code in `index.js` / `base/*.js`: CommonJS `require`/`module.exports`, 2-space indent, single quotes, semicolons, `const`/arrow functions.
- Commit/changelog style: informal Conventional-Commits-like prefixes seen in `CHANGELOG.md` — `chore:`, `feature:`, `fix:`. Follow this prefix style for changelog entries and commit subjects when relevant.

## Roles → agents
| Role | Agent |
|------|-------|
| investigator | Explore |
| fixer | general-purpose |
| reviewer | general-purpose (no dedicated `code-reviewer` agent in this repo/session; alternatively use the `/code-review` skill) |
| verifier | general-purpose (manual verification only — no test suite or browser harness; verifier should reason through code paths and, if possible, syntax-check with `node --check`) |

## Ship mechanics
- Create PR: `gh pr create` (repo: `ianiv/homebridge-sun-azimuth`)
- Draft-first: no — both prior PRs (#1, #2) were created as regular (non-draft) PRs and merged directly
- Evidence in PR: n/a — no screenshots/test-output convention observed; keep PR body to a concise summary + test plan (manual verification steps, since no automated tests exist)
- Ticket transition on ship: n/a (no ticket system)
- PR-authoring conventions: none documented; follow the `chore:`/`feature:`/`fix:` prefix style from `CHANGELOG.md` for the PR title where applicable

## Notes / open questions
- TODO: This is a tiny, config-less JS project — the pipeline's "build gate" step will effectively be a no-op syntax check. If the user later adds TypeScript/ESLint/tests, re-run `orchestrate-fix-init` to pick up the new gate.
- TODO: No `code-reviewer` subagent is available in this environment — reviewer role uses general-purpose. If a dedicated review agent becomes available, rebind here.
- `package.json` `repository.url`/`bugs.url`, `README.md`, and `config.schema.json` now point to `ianiv/homebridge-sun-azimuth` (updated 2026-07-02; previously pointed to the upstream fork `awaescher/homebridge-sun-azimuth`).
